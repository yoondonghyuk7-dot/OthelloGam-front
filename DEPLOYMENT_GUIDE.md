# 오셀로 게임 배포 가이드

## 🎮 빠른 시작

### 현재 방식 (가장 간단)

**네, 클라이언트도 같은 코드를 받아서 실행해야 합니다.**

하지만 서버와 클라이언트는 **다른 방식으로 실행**합니다:

---

## 📦 배포 방법

### 1️⃣ 서버 컴퓨터 (게임 호스트)

```bash
# 프로젝트 폴더에서
mvn clean compile

# 서버만 실행
java -cp target/classes org.example.server.ServerLauncher
```

또는 IDE에서 `ServerLauncher.java` 실행

**서버 IP 확인:**
- Windows: `ipconfig` → IPv4 주소 확인
- Linux/Mac: `ifconfig` 또는 `ip addr`

---

### 2️⃣ 클라이언트 컴퓨터 (다른 컴퓨터)

**같은 코드를 받아서:**

```bash
# 프로젝트 폴더에서
mvn clean compile

# 게임 실행
java -cp target/classes org.example.Main
```

또는 IDE에서 `Main.java` 실행

**온라인 대전 시:**
1. "온라인 1:1 대전" 클릭
2. 서버 IP 입력 (예: `192.168.0.100`)
3. 매칭 대기

---

## 🔧 더 나은 방법: JAR 파일로 배포

### 빌드
```bash
mvn clean package
```

### 배포 파일
- `target/OthelloGame-1.0-SNAPSHOT.jar` 생성됨

### 서버 실행
```bash
java -cp OthelloGame-1.0-SNAPSHOT.jar org.example.server.ServerLauncher
```

### 클라이언트 실행
```bash
java -jar OthelloGame-1.0-SNAPSHOT.jar
```

---

## 📋 요약

| 항목 | 서버 | 클라이언트 |
|------|------|-----------|
| 실행 파일 | `ServerLauncher.java` | `Main.java` |
| 필요한 것 | 같은 코드 | 같은 코드 |
| 실행 방법 | 서버만 실행 | 게임 실행 |
| IP 설정 | 자동 (서버 IP) | 수동 입력 |

---

## 💡 팁

1. **같은 네트워크**: 서버 IP만 알면 접속 가능
2. **다른 네트워크**: 포트포워딩 필요
3. **테스트**: 같은 컴퓨터에서 둘 다 실행 가능 (IP: `127.0.0.1`)

